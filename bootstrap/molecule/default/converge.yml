---
- name: Converge
  hosts: all
  become: true
  gather_facts: yes
  tasks:
    - name: "Include dotfiles.bootstrap"
      include_role:
        name: bootstrap
      vars:
        setup_user: jane
        github_token: secret
        create_ssh_keypair: true

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

layerrule = blur, waybar
layerrule = ignorezero, waybar
layerrule = ignorealpha 0.15, waybar
layerrule = blurpopups, waybar

layerrule = blur, notifications
layerrule = ignorezero, notifications
layerrule = animation slide top, notifications

layerrule = noanim, selection

# layerrule = blur, logout_dialog
# layerrule = animation fade, logout_dialog

layerrule = blur, wofi
layerrule = blur, thunar
# layerrule = ignorezero, wofi
# layerrule = ignorealpha 0.76, wofi

# Ignore maximize requests from apps. You'll probably like this.
windowrulev2 = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrulev2 = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

windowrule = float, title:^(pavucontrol)$
windowrule = stayfocused, title:wofi
windowrule = animation popin 40%, class:wofi


# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
workspace = w[tv1], gapsout:0, gapsin:0
workspace = f[1], gapsout:0, gapsin:0
windowrulev2 = bordersize 0, floating:0, onworkspace:w[tv1]
windowrulev2 = rounding 0, floating:0, onworkspace:w[tv1]
windowrulev2 = bordersize 0, floating:0, onworkspace:f[1]
windowrulev2 = rounding 0, floating:0, onworkspace:f[1]


windowrulev2 = float, class:^(zen-beta)$, title:.*Save(Image|file|As).*
windowrulev2 = size 800 450, class:^(zen-beta)$, title:.*Save(Image|file|As).*
windowrulev2 = pin, class:^(zen-beta)$, title:.*Save(Image|file|As).*

# windowrulev2 = float, class:^(zen-beta)$, title:.*(Bitwarden)*
# windowrulev2 = size 450 800, class:^(zen-beta)$, title:.*(Bitwarden)*
# windowrulev2 = pin, class:^(zen-beta)$, title:.*(Bitwarden)*
